#include <stdio.h>

int main()
{
  int A,a=0,b=0,total=0;
  scanf("%d",&A);
  while(0<=A<=99){
    if(A<10)
      total=0+A;
    else if(A==total)
      a=total%10;
      b=total/10;
      total=a+b;
    else (A==total)
    printf("%d",len(total));}
  return 0;
}
